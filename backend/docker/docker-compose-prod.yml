name: sgi-backend-prod
services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile.prod # Ligamos el dockerfile personalizado para producción
    ports:
      - "3001:3001" # Mapea el puerto del contenedor al de tu máquina
    env_file:
      - ../.env.production # Archivo con las variables de entorno